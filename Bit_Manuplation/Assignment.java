package Bit_Manuplation;
import java.util.*;

public class Assignment {
    public static void main(String[] args) {
        // swapping problem ...

        // Scanner sc=new Scanner(System.in);
        // int x=sc.nextInt();
        // int y=sc.nextInt();
        // System.out.println("Before swapping the value : X="+x+" and Y="+y);
        // x=x^y;
        // y=x^y;
        // x=x^y;
        // System.out.println("After swapping the value : X="+x+" and Y="+y);        

        // System.out.println(x + " + "+ 1 + " is "+ -~x);
        for(char ch='A';ch<='Z';ch++){
            System.out.print((char)(ch|' '));
        }
    }
}
